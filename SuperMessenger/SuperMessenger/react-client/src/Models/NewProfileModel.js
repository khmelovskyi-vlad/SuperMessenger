export default class NewProfileModel {
  constructor(firstName, lastName, haveImage, contentId) {
    this.firstName = firstName;
    this.lastName = lastName;
    this.haveImage = haveImage;
    this.contentId = contentId;
  }
}