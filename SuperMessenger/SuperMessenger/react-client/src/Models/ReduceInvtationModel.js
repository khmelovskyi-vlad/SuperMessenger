export default class ReduceInvtationModel {
  constructor(groupId, invitedUserId, inviterId) {
    this.groupId = groupId;
    this.invitedUserId = invitedUserId;
    this.inviterId = inviterId;
  }
}